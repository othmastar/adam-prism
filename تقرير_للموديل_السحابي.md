# تقرير المشاكل الحالية — Adam Prism v2 (الواقع الفعلي)

## مقدمة
هذا التقرير كتبه **وكيل GLM-5.2 المحلي (OpenCode)** بعد تحليل الكود الموجود فعلياً والمشاكل الحقيقية اللي بنواجهها. الهدف: تعطيك **صورة دقيقة** عن الواقع عشان تقدم حل أفضل من الـ Desktop v3.

---

## 1. المشاكل الحقيقية (اللي بتوقف الشغل)

### 1.1 WebSocket يتصل ويقفل فوراً
- **المشكلة:** `ws://localhost:8000/ws/chat` → `[accepted]` → `connection open` → `connection closed` (في أقل من ثانية)
- **النتيجة:** الفرونت بيحاول يبعت عبر WS، الـ promise يعلق 120 ثانية، الـ REST fallback مش بيشتغل لأن الـ WS مش فشل — هو اتقفل. الـ promise مش بيتresolve أبداً.
- **مكان الكود:**
  - `frontend/web-ui/src/components/adam/chat-interface.tsx:787-790` — logic بتاع WS→REST fallback
  - `backend/adam/api/server_minimal.py` — WebSocket handler
- **سبب محتمل:** الـ WebSocket handler عنده `try/except` كاتش كل الأخطاء ومبيرجعش حاجة. أو الـ proxy بتاع Next.js مش بيعمل upgrade للـ WS صح.

### 1.2 الرسايل مش بتوصل للـ backend من الـ UI
- **الدليل:** backend logs مفيش ولا `POST /api/chat` من `localhost:3000` — كلها health checks بس
- **السبب:** `getFastApiUrl()` بترجع `""` (لأن localStorage فاضي)، فـ fetch بيبعت على `/api/chat` relative URL. دايمفترض يشتغل عبر Next.js proxy.
- **المشكلة:** الـ proxy شغال (health checks بتوصل)، بس الرسايل مش بتوصل. ممكن لأن الـ WS بياخد priority وبيقفل قبل ما الـ REST يجرب.

### 1.3 التول لوب (أُزيل لكن كان كارثة)
- **كان:** 4 أدوار تول كول، كل دور 3-5 ثواني على GPU. 48-60 ثانية للرسالة الواحدة.
- **الآن:** أُزيل بالكامل. Flask بيعمل generation واحدة (`/generate`).
- **الدرس:** Gemma 4 12B مش fine-tuned على tool calling. ميقدرش يطلع `<tool_call>` منظم.

### 1.4 الـ context window بيقطع رسالة المستخدم
- **كان:** `truncation_side='right'` — بيقطع آخر الكلام (رسالة المستخدم + الملف)
- **أصبح:** `truncation_side='left'` — بيقطع أول الكلام (system prompt القديم)
- **لسه:** `max_length=16384` — لو رسالة المستخدم + الملف أكبر من كده، بردو هيتقطع
- **المشكلة:** العربي 0.3 token/char. 16384 tokens ≈ 50K حرف عربي. لو في ملف كبير، بيتقطع.

### 1.5 ما فيش embedding model شغال
- `nomic-embed-text` مش مثبت في Ollama
- الـ `store.py` الحالي بيستخدم SQLite FTS5 بس (keyword search, مش semantic)
- الـ LTM مش بيشتغل فعلياً

---

## 2. المشاكل التجميلية (404s مش مؤثرة)

- `GET /api/skills/list` — مش موجود، بيظهر 404 في الكونسول
- `GET /api/plugins` — مش موجود
- `GET /api/subagents` — مش موجود
- `GET /api/predict/status` — مش موجود
- `GET /api/predict/last` — مش موجود

كلها بانلات جانبية في الفرونت بتجيب داتا مش موجودة. **ما تأثرش على الشات.**

---

## 3. تقييم حل Desktop v3

### الحاجات الصح:
| الميزة | التحليل |
|--------|---------|
| LTM بـ nomic-embed-text (vector + BM25) | **ممتاز.** دا genuine upgrade. hybrid search أحسن بكتير من FTS5 لوحده. |
| Context compressor بـ auxiliary LLM | **ممتاز.** دا الحل الحقيقي لـ "أنا آدم". بيحافظ على آخر 8 رسائل ويلخص الباقي. |
| Memory decay + consolidation | **ممتاز.** الذاكرة تضعف مع الوقت لو مش بتتستخدم، والذكريات المتشابهة تندمج. |
| Auxiliary model (qwen2.5:0.5b) | **ممتاز.** 500MB بس، يعمل summarization + title generation + memory extraction. |

### الحاجات المش مشكلة (لكن مش urgent):
| الميزة | التحليل |
|--------|---------|
| PyWebView desktop launcher | مش محتاجينه دلوقتي. الفرونت بتاع Next.js شغال ومطور كويس. |
| 40+ API endpoint | Skills, Predict, Diagnostics مش مستعجلة. 10 endpoints كافية. |
| Playwright browser | حاجة جامدة بس مش core. الفرونت بيعمل basic fetch كويس. |
| XML tool calling | Gemma 4 مش هينفذه صح. محتاج fine-tuning أو model مخصص. |

### الحاجات الغلط:
| الميزة | المشكلة |
|--------|---------|
| استبدال Next.js بـ PyWebView | أهدر مجهود المستخدم في الفرونت. ممكن نضيف PyWebView كـ optional mode مش basic. |
| افتراض إن Gemma 4 هيطلع tool_call منظم | مش هيحصل. الموديل مش مدرب عليه. |
| تجاهل مشكلة WebSocket | الـ v3 عنده WebSocket بردو لكن بنفس المشكلة. |

---

## 4. الأولويات الحقيقية (من الأهم)

1. **إصلاح WebSocket** — ياما نشيله خالص (REST only) أو نصلحه
2. **تثبيت nomic-embed-text** — `ollama pull nomic-embed-text`
3. **تثبيت qwen2.5:0.5b** — `ollama pull qwen2.5:0.5b`
4. **دمج long_term.py مع vector embeddings** — من v3
5. **دمج context compressor** — من enhanced_engine.py (دا بيحل مشكلة "أنا آدم")
6. **إضافة stubs للـ 404 endpoints** — عشان تختفي من الكونسول
7. **تثبيت qwen2.5:3b** — عشان fallback يشتغل

---

## 5. طلب محدد للموديل السحابي

لو عاوز تساعد بجد، من فضلك:

**مطلوب:** حل لكل مشكلة من المشاكل ال 7 في القسم 1، مع كود محدد للتعديل.

**مش مطلوب:** Desktop app, 40 endpoint, Playwright, tool calling loop.

**السيناريو المستهدف:**
- GPU: RTX 3060 12GB (10.5GB مستخدم حالياً)
- Linux، Python 3.10+
- Gemma 4 12B 4-bit على port 7861 (LoRA server)
- Next.js frontend على port 3000 (REST فقط، بدون WebSocket)
- Backend على port 8000
- nomic-embed-text للـ embeddings
- qwen2.5:0.5b للـ auxiliary tasks
- qwen2.5:3b للـ fallback
- **no desktop app, no Playwright, no tool calling loop**

---

*تقرير من OpenCode (وكيل GLM-5.2 المحلي) إلى GLM-5.2 السحابي*
*24 يونيو 2026*
